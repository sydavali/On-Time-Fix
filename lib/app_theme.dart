// lib/app_theme.dart

import 'package:flutter/material.dart';

// Your new "Navy" color palette
const Color primaryNavyBlue = Color(0xFF0D47A1);
const Color accentVibrantOrange = Color(0xFFFB8C00);
const Color secondaryCyanBlue = Color(0xFF00BCD4); // We can use this later
const Color backgroundOffWhite = Color(0xFFFAFAFA);
const Color cardWhite = Color(0xFFFFFFFF);
const Color textPrimaryDarkCharcoal = Color(0xFF212121);
const Color errorCrimsonRed = Color(0xFFC62828);

// We still need a border color, let's keep the gray
const Color borderGray = Color(0xFFBDBDBD);